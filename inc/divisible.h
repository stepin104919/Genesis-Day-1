/**
* @file divisible.h
* Program to check if the first number is perfectly divisible by second
*
*/
#ifndef __DIVISIBLE_H__
#define __DIVISIBLE_H__

/**
* Check the divisibility of a number upon another number
* @param[in] number which is to be divided
* @param[in] number which is to be used to divide
* @return 1 if perfectly divisible
* @note Returns -1 if divisor is zero
*/
int divisible(int number1, int number2);

#endif /* #ifndef __FACTORIAL_H__ */
